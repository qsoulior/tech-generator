paths:
  versionCreateFrom:
    x-ogen-operation-group: VersionCreateFrom
    post:
      operationId: versionCreateFrom
      summary: Создать версию шаблона из другой версии
      parameters:
        - $ref: "../common.yml#/components/parameters/UserID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionCreateFromRequest"
      responses:
        201:
          description: Created
        400:
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "../common.yml#/components/schemas/Error"

components:
  schemas:
    VersionCreateFromRequest:
      type: object
      required:
        - templateID
        - versionID
      properties:
        templateID:
          type: integer
          format: int64
          description: ID шаблона
        versionID:
          type: integer
          format: int64
          description: ID версии
